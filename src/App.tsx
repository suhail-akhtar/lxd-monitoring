// App.tsx will be minimal, as AppRouter handles the main structure
import React from 'react';

const App: React.FC = () => {
  return null; // AppRouter handles the rendering
};

export default App;
